import { Meta } from '@storybook/blocks';

<Meta title="Hooks/PlanetUpdate" />

# hooks/PlanetUpdate
The PlanetUpdate hook will dispatch an action to update the planet in the store. The action will increase the number of available armies if a planet is owned by a player.
