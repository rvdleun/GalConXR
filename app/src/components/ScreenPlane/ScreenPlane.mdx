# ScreenPlane

This component will render a simple plane. We can then add Screen components as children to it which will then be rendered on top of the canvas.

Any interaction with the ScreenPlane will be forwarded to the Screen components.
