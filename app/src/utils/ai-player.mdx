import { Meta } from '@storybook/blocks';

<Meta title="Utils/AiPlayer" />

# Utils/AiPlayer
The AiPlayer utilities file contains functions that will return a new ArmyMovement. The ArmyMovement can then be added to the store to simulate a player action.
